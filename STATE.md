# Current State

This file records what has actually been verified on the real hardware — not
plans, not assumptions. Every section carries a verification date and the
command/method used to confirm it. If a fact hasn't been verified, mark it
`[UNVERIFIED]` rather than guessing or leaving it implied as fact.

## Nodes

### `<hostname>`

- Last verified: `<date>` — verified via: `<command>`
- Hardware:
  - CPU:
  - RAM:
  - GPU:
- Role:
- Proxmox version:

## Network

- Last verified: `<date>` — verified via: `<command>`
- LAN subnet:
- Gateway:
- Node IPs:
- VPN / overlay (if any):

## Storage

- Last verified: `<date>` — verified via: `<command>`
- Per-node disks:
- Pools (type, members):

## Cluster

- Last verified: `<date>` — verified via: `<command>`
- Cluster exists: yes / no
- Nodes joined:
- HA enabled: yes / no

## VMs / LXCs currently running

- Last verified: `<date>` — verified via: `<command>`
- (list of running VMs/LXCs with id, node, purpose)

## Services currently running

- Last verified: `<date>` — verified via: `<command>`
- (list of services/workloads actually deployed, as opposed to planned)
